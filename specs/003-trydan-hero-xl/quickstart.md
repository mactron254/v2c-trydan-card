# Quickstart: Trydan Hero XL

```bash
corepack pnpm --version
corepack pnpm check
corepack pnpm demo
```

Validate `display_mode` values `standard`, `compact` and `ultra_compact` at 280, 320, 359, 520 and 768 px. Repeat with `show_charger: false`, light/dark themes and all supported languages.
